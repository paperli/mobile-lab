# Server Environment Variables
# PORT - Server port (default: 3000, Render will provide this automatically)
PORT=3000

# ALLOWED_ORIGINS - Comma-separated list of allowed origins for CORS
# For local network access, add your machine's IP address
# Find your IP: ifconfig en0 (Mac), ipconfig (Windows), ip addr show (Linux)
#
# Local development with HTTPS (recommended):
# ALLOWED_ORIGINS=https://localhost:5173,https://localhost:5174,http://localhost:5173,http://localhost:5174
#
# Local network access:
# ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,http://YOUR_IP_HERE:5173,http://YOUR_IP_HERE:5174
#
# Production (Render):
# ALLOWED_ORIGINS=https://mobile-lab-tv.onrender.com,https://mobile-lab-mobile.onrender.com
ALLOWED_ORIGINS=https://localhost:5173,https://localhost:5174,http://localhost:5173,http://localhost:5174

# Frontend Environment Variables (for both TV and Mobile)
# VITE_SERVER_URL - Backend server URL
#
# Local development with HTTPS (recommended):
# VITE_SERVER_URL=https://localhost:3000
#
# Network access with HTTPS:
# VITE_SERVER_URL=https://YOUR_IP_HERE:3000
#
# Production (Render):
# VITE_SERVER_URL=https://mobile-lab-server.onrender.com
VITE_SERVER_URL=https://localhost:3000

# VITE_MOBILE_URL - Mobile controller URL (used for QR code on TV)
# NOTE: This is now OPTIONAL! The TV app auto-detects the environment:
#   - On Render: Automatically uses mobile-lab-mobile.onrender.com
#   - On local network: Uses the same IP as TV with port 5174
#   - On localhost: Uses localhost:5174 or this env var if set
#
# Only set this if you need to override the auto-detection:
# VITE_MOBILE_URL=https://localhost:5174
# VITE_MOBILE_URL=https://YOUR_IP_HERE:5174
