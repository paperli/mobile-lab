# TV Frontend Environment Variables

# VITE_SERVER_URL - Backend server URL (optional, auto-detects if not set)
# For local development with HTTPS (recommended):
VITE_SERVER_URL=https://localhost:3000

# For production (Render):
# VITE_SERVER_URL=https://mobile-lab-server.onrender.com

# VITE_MOBILE_URL - Mobile controller URL for QR code
# This URL will be encoded in the QR code displayed on the TV screen
#
# For local development on same computer:
VITE_MOBILE_URL=https://localhost:5174

# For network access (recommended for testing on real phones):
# Replace 192.168.20.40 with your local IP address
# Find your IP: ./setup-https.sh will detect it, or use:
#   Mac/Linux: ifconfig | grep "inet " | grep -v 127.0.0.1
#   Windows: ipconfig
# VITE_MOBILE_URL=https://192.168.20.40:5174

# For production (Render):
# VITE_MOBILE_URL=https://mobile-lab-mobile.onrender.com
