# TV Frontend Environment Variables

# VITE_SERVER_URL - Backend server URL (optional, auto-detects if not set)
# Find your local IP address:
#   Mac: ifconfig en0 | grep "inet " | awk '{print $2}'
#   Windows: ipconfig (look for IPv4 Address)
#   Linux: ip addr show | grep "inet "
#
# Local development with HTTPS (recommended):
# VITE_SERVER_URL=https://localhost:3000
#
# Network access with HTTPS:
# VITE_SERVER_URL=https://YOUR_IP_HERE:3000
#
# Production (Render):
# VITE_SERVER_URL=https://mobile-lab-server.onrender.com
VITE_SERVER_URL=https://localhost:3000

# VITE_MOBILE_URL - Mobile controller URL for QR code
# This URL will be encoded in the QR code displayed on the TV screen
#
# NOTE: This is now OPTIONAL! The TV app automatically detects:
#   - On Render (*.onrender.com): Uses mobile-lab-mobile.onrender.com
#   - On local network (192.168.x.x): Uses same IP with port 5174
#   - On localhost: Uses localhost:5174 or this env var if set
#
# Only set this if you need to override the auto-detection:
# VITE_MOBILE_URL=https://localhost:5174
# VITE_MOBILE_URL=https://YOUR_IP_HERE:5174
VITE_MOBILE_URL=https://localhost:5174
